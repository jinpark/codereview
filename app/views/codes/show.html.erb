<%= render @code %>

<br>
Add New Comment
<form action="<%= comments_url %>" method="post">
	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
	<input type="hidden" name="comment[code_id]" value="<%= @code.id %>">
	<textarea rows='6' cols='100' name="comment[content]"></textarea>
	
	<input type="submit" name="Add new comment">
</form>


<ul>
	<% @comments[nil].each do |parent| %>
			<%= render partial: "comments/comment", locals: {comment: parent, comments: @comments} %>
	<% end %>
</ul>

<%= link_to "Back to index", root_path %>

<script>

$(document).ready(function() {
	$('.resizable').resizable()
	
});

</script>